# Flask Application Secret Key for Session Security
SECRET_KEY=c3be9d9a1f2e3d4c5b6a7f8e9d0c1b2a

# MySQL Connection String (MySQL 8 required for JSON support)
# We append ?ssl_disabled=True to prevent SSL thread socket errors on concurrent background runs.
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/excel_cms?ssl_disabled=True

# Flask environment configuration
FLASK_ENV=development
FLASK_APP=run.py
